{{{
		// CPL
		baseHandlers[0x2F] = new Handler() {
			public void handle(int instr) {
				registers.reg[_A] ^= 0xff;
				adjustFlag(F_H, true);
				adjustFlag(F_N, true);
				tStates += 4;
			}
		};
}}}